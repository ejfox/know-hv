export default defineAppConfig({
  ui: {
    icons: {
      dynamic: true
    },
    button: {
      default: {
        size: 'md',
        color: 'gray',
        variant: 'ghost'
      }
    }
  }
})
